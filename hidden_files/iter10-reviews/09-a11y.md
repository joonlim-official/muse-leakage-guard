# Iteration-10 review — Accessibility specialist

**Target:** `~/workspace/skills/muse-leakage-guard`, HEAD `653bdd9`.
**Mode:** REVIEW-ONLY; reviewed the report generator's card rendering and
the planned suite-C change for new a11y surface. No repo writes.

**Lens:** does the iteration-10 change introduce any new accessibility
surface, and does the existing surface hold?

## Verdict summary

**2 findings, both P3.** The probe set introduces no new interactive
elements, no new card, no new visual language — the six new checks render
as list items inside the existing card-C disclosure, which already has the
required semantics (native `<button>`, `aria-expanded`, named region,
`role="region"`, skip link, red-card pre-expansion). The work is
verification, not new development.

## Findings

### A11Y-1 (P3) — no new a11y surface; standard screenshot QA still required

**What.** Card C's evidence list grows by six `<li>` items in the existing
`<ul class="evidence">`. Tri-state grouping is suite-B-only, so no new
grouping headings appear. The "Known gaps" subtotal logic keys on
"residual" in the suite name — suite C's rename must not introduce that
word (it won't: "controls verified in place" contains neither "residual"
nor "gap"). The one rendering path to eyeball: the card-C callout becomes
"🟢 Attack-surface coverage: 14 of 14 green" (or with skips) — longer
evidence, same callout shape. Recommend the standard local headless-Chrome
QA (desktop + mobile, light + dark if cheap) on the regenerated report to
confirm the longer card still collapses/expands cleanly and the findings
anchor links still resolve.

### A11Y-2 (P3) — terminal output: keep probe names short

**What.** The terminal audit wraps check lines at WIDTH-4 with hanging
indent (`wcheck`), which is screen-reader-friendly as long as check names
are single logical lines. Probe names like "memory-guard: secret blocked
(rc=1)" wrap to a second indented line rather than breaking mid-token —
acceptable. Just keep the names terse and never embed the fixture content
in the name (rc numbers only), consistent with the existing evidence
hygiene.
